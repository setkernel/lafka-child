<?xml version="1.0"?>
<ruleset name="Lafka Child Theme">
	<description>WordPress coding standards for Lafka Child Theme</description>

	<!-- What to scan -->
	<file>.</file>

	<!-- Show progress and sniff codes in all reports -->
	<arg value="ps"/>
	<arg name="colors"/>
	<arg name="extensions" value="php"/>

	<!-- Strip the filepaths down to the relevant bit -->
	<arg name="basepath" value="."/>

	<!-- Rules -->
	<rule ref="WordPress-Extra">
		<!-- Allow short array syntax -->
		<exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
	</rule>

	<!-- Cross-version PHP compatibility against the 8.1 floor. PHPCompatibility
	     10 (alpha, same as lafka-plugin) is required: 9.3.5 has no PHP 8.x sniffs. -->
	<config name="testVersion" value="8.1-"/>
	<rule ref="PHPCompatibilityWP"/>

	<!-- WordPress text domain -->
	<config name="text_domain" value="lafka"/>

	<!-- Minimum supported WordPress version (WP 6.6 = mid-2024 baseline) -->
	<config name="minimum_wp_version" value="6.6"/>

	<!-- Exclude vendor directories -->
	<exclude-pattern>/vendor/*</exclude-pattern>
	<exclude-pattern>/node_modules/*</exclude-pattern>
	<!-- PHPUnit tests use their own PascalCase naming convention, not WP kebab-case. -->
	<exclude-pattern>/tests/*</exclude-pattern>
</ruleset>
